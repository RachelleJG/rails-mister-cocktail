<h1><%= @cocktail.name %></h1>

<h3><% @cocktail.doses.each do |dose| %></h3>
  <%= dose.description %>
  <%= dose.ingredient.name %>
  <% link_to(dose_path(dose)) do %>
    <i class="fa fa-trash"> </i>
  <% end %><br>
<% end %>

  <%# link_to ' ', dose_path(dose), class: 'btn btn-danger', method: :delete %>


<%= link_to "Add Dose", new_cocktail_dose_path(@cocktail), class: 'btn btn-info' %><br>
<%= link_to "Back to Cocktails", cocktails_path, class: 'btn btn-warning'%><br>
<%= link_to "Remove Cocktail", cocktail_path(@cocktail),  class: 'btn btn-danger', method: :delete, data: { confirm: "are you sure?"}  %>
